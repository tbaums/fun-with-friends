LOCAL ISSUES MODE: issue references in commits and reports read LI-<num> and live in a local tracker, not this repo's GitHub issues. Your branch mechanics are unchanged; when filing a tracking issue for a red e2e, use the issue-tracker commands in this prompt (already pointed at the local store).
