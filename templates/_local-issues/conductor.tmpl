LOCAL ISSUES MODE — everything is local and the remote is NOT ours to write: NEVER fetch/pull/push; drop those from every step. Ahead check: git rev-list --count __INTEGRATION__..__STAGING__ (local refs, no origin/ prefix). Run e2e on a DETACHED checkout so you never hold the shared branch: git switch --detach __STAGING__. Promote locally: git switch __INTEGRATION__ (if another worktree holds it, defer to next cycle) && git merge --ff-only __STAGING__ — then free it: git switch --detach __INTEGRATION__. When filing a tracking issue for a red e2e, use the issue-tracker commands in this prompt (already pointed at the local store; numbers read LI-<num>).
