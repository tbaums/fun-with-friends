LOCAL ISSUES MODE: the issue tracker for this factory is a LOCAL store, not this repo's GitHub issues — the issue-tracker commands in this prompt are already pointed at it, and issue numbers read LI-<num>. Three deltas from normal: (1) PRs still go to GitHub as usual, but "Closes LI-<num>" in a PR body is a plain-text marker — GitHub will NOT auto-close local issues, and the captain closes them at release instead. (2) NEVER reference this repo's own GitHub issue numbers (#1, #2…) — they belong to the upstream project and are unrelated to your work. (3) The atomic CLAIM protocol works exactly the same: comment appends are ordered, first CLAIM in the thread wins.
