LOCAL ISSUES MODE — the tracker is a LOCAL store (issue numbers read LI-<num>; the issue-tracker commands in this prompt are already pointed at it), and this repo's remote is NOT ours to write: NEVER run `git push`, `gh pr create`, `gh pr ready`, or ANY command that writes to the remote — a pre-push guard blocks pushes and a gh-write guard blocks mutating gh commands, and only the HUMAN may authorize a push (via the captain). The PR-based steps in your cycle are REPLACED by this fully local flow:
- CLAIM exactly as instructed (the atomic CLAIM comment IS the claim — there is no draft-PR step). After winning, branch locally WITHOUT touching the shared branches: git switch -c impl__ID__/issue-<num>-<slug> __STAGING__ (this reads __STAGING__; it never checks it out).
- BUILD in small commits with the Co-Authored-By trailer; keep the gate (__GATE__) green. Skip every push/PR instruction.
- HANDOFF: when done, comment on the local issue: "READY-FOR-REVIEW impl__ID__ · branch impl__ID__/issue-<num>-<slug> · <sha> · <one line on what qa__ID__ should verify>". That replaces gh pr ready.
- FEEDBACK: qa__ID__ replies on the issue with "CHANGES-REQUESTED impl__ID__: <details>" — fix on the SAME branch and post a fresh READY-FOR-REVIEW with the new sha. So your cycle's step 1 becomes: scan your claimed issues' comments for CHANGES-REQUESTED; step 2: a posted READY-FOR-REVIEW with no qa reply yet means you wait (report and end the cycle); step 3: survey + claim as instructed.
- Dup detection against shared branches works unchanged (the LI-<num> markers land in local squash commits on __STAGING__/__INTEGRATION__). Never reference the upstream repo's GitHub issue numbers; never merge your own branch; one issue = one branch, at most one in flight.
