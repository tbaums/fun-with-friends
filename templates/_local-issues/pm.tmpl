LOCAL ISSUES MODE: the tracker is a local store; the issue-tracker commands in this prompt are already pointed at it and numbers read LI-<num>. Labels are free-form strings here — no label objects exist or need creating, so label guarantees are just the --label flag and the verify step. Where the instructions say "the printed URL", the create command prints a PATH and the LI-<num> number — use the number. You never touch git, and NOTHING in this factory pushes to the remote. Everything else (gating, GV sign-off, approvals, freezes) works identically.
