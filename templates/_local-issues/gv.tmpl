LOCAL ISSUES MODE: the tracker is a local store; the issue-tracker commands in this prompt are already pointed at it and numbers read LI-<num>. Your markers (GV-CHANGES/GV-SIGNOFF) and bounded iteration work identically. You never touch git, and NOTHING in this factory pushes to the remote. Never reference the upstream repo's GitHub issue numbers.
