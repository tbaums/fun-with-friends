LOCAL ISSUES MODE — the tracker is a LOCAL store (numbers read LI-<num>), and this repo's remote is NOT ours to write: NEVER push, never use gh pr commands — there are NO pull requests here. IGNORE the PR-based queue in your prompt; this is your loop instead:
1. QUEUE: gh issue list --search "is:open" --json number,title — then read comments; your work is every issue whose LATEST "READY-FOR-REVIEW impl__ID__ …" comment names a sha you have not yet reviewed (your own later MERGED/CHANGES-REQUESTED reply means it's done).
2. REVIEW in YOUR worktree without stealing the branch: git switch --detach <branch from the comment>; run the fast gate: __GATE__. Apply the same behavior bar as your main prompt.
3. GREEN → merge it into local __STAGING__: git switch __STAGING__ (if that fails because another worktree holds it, defer this merge to the next cycle) && git merge --squash <branch> && git commit -m "<issue title> (LI-<n>)" -m "Closes LI-<n>" -m "Co-Authored-By: Claude <noreply@anthropic.com>" — then IMMEDIATELY free the shared branch: git switch --detach __STAGING__. Delete the work branch (git branch -D <branch>), and comment on the issue: "MERGED LI-<n> to __STAGING__ at <merge sha>". The issue stays open until release.
4. RED → comment "CHANGES-REQUESTED impl__ID__: <the EXACT failing output — never paraphrase>" on the issue. Do NOT merge; do not edit their code.
Everything else (one reviewer per impl, behavior contract, no e2e) is unchanged. Never reference the upstream repo's GitHub issue numbers.
