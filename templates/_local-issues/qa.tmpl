LOCAL ISSUES MODE: the tracker is a local store; issue-tracker commands in this prompt are already pointed at it and numbers read LI-<num>. Keep "Closes LI-<n>" in the squash commit body exactly as instructed — it will not auto-close anything (the captain closes local issues at release); it is the traceability marker that ties the commit to the local issue. Never reference this repo's own GitHub issue numbers.
