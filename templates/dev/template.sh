#!/usr/bin/env bash
# shellcheck disable=SC2034  # consumed by lib.sh after the profile loads
# fwf template: classic feature factory — ideas → ship pipeline (the default)
# No config overrides: the dev factory uses the stock defaults (3 pairs).
