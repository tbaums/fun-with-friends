{
  "_comment": "issue #220: the committed source of truth for required-status-check branch protection on staging/integration/main. fwf-branch-policy.sh diffs LIVE GitHub protection settings against this file -- drift (a setting that no longer matches) is a RED, surfaced on fwf dash. Renaming a CI job means updating required_contexts here IN THE SAME COMMIT (issue #220 edge case: a silently-renamed context never reports, and GitHub treats never-reported as pending forever -- every PR blocks with no failing check to point at).",
  "required_contexts": [
    "shellcheck + syntax",
    "functional suite (ubuntu-latest)",
    "functional suite (macos-latest)",
    "dash crate (rust)"
  ],
  "strict": false,
  "enforce_admins": true,
  "branches": ["staging", "integration", "main"]
}
