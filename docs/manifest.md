# The manifest

Every key of `.fwf/fwf.toml`: its type, default, and an example.
The reference you keep open while editing a manifest.

Content pending — see the issue tracker.

## See also

- [`index.md`](index.md) — the fwf 1.0 documentation index.
